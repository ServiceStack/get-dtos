// import { $ } from 'bun'
import { describe, it, expect } from 'bun:test'

describe("basic", () => {
    it ("should work", () => {
        // const out = await $`node ./dist/bin.js typescript https://openai.servicestack.net`.text()
        // console.log('out', out)
        // expect(1).toEqual(1)
    })
})
